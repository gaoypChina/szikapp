import 'package:flutter/material.dart';

class CleaningApplyView extends StatelessWidget {
  const CleaningApplyView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Apply');
  }
}
