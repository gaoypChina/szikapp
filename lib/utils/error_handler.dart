import 'package:flutter/material.dart';

class ErrorHandler {
  static Widget buildBanner() {
    return Container(
        //Ide jön a dizájn
        );
  }

  static Widget buildInset() {
    return Container(
        //Ide jön a dizájn
        );
  }
}
